name: mypy

on:
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - name: checkout code
      uses: actions/checkout@v4

    - name: set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install mypy
        pip install -r requirements.txt
        pip install -r dhscanner.infra/app/requirements.txt
        pip install -r dhscanner.infra/logger/requirements.txt
        pip install -r dhscanner.infra/workers/requirements.txt

    - name: Run mypy
      run: mypy --install-types --non-interactive $(git ls-files '*.py')